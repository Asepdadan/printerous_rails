<%= @page_title = "Form Organization" %>

  <% content_for :stylesheets do %>
   
  <% end %>
 
  <% content_for :link_href do %>

  <% end %>

  <% content_for :content_html do %>
   
   <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <div class="row">
                        <div class="col-md-10">
                            Form User
                        </div>

                        <div class="col-md-2">
                            <a href="/user" class="btn btn-success float-right">Kembali</a>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    
                    <%= form_with model: @user , :url => user_create_path, :method => "post", local: true do |f| %>
                    
                    <%= render partial: "user/form", locals: {f: f} %>

                    <% end %>
                </div>
            </div>
        </div>

        <div class="col-md-3"></div>
   </div>

  <% end %>


    <% content_for :script do %>

     <% end %>
  
